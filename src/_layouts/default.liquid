<!DOCTYPE html>
<html lang="en">
    <head>
      {% include "head.liquid" %}   
    </head>
    <body>
      <header>
        {% include "header.liquid" %}
      </header>
     <nav>
       {%- include "nav.liquid" -%}
     </nav>
     <main>
     <div id="ToC"></div>
     {% if page.data.image %}
      <img src="../media/{{page.data.image}}">
     {% endif %}
        {{ page.content }}
      {% if page.data.type  %}
        {%- if page.data.type == 'portal' -%}
          {%- include "portal.liquid" -%}
        {%- endif -%}
      {% endif %}

      </main>
      <footer>
        {%- include "footer.liquid" -%}
     </footer>
    <script src="../links/scripts/toc.js"></script>
    </body>
</html>
